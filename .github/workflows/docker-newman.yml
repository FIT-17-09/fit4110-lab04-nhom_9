name: Lab04 Docker and Newman Verification

on:
  push:
  pull_request:

jobs:
  docker-newman:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install Node dependencies
        run: npm ci

      - name: Lint OpenAPI contract
        run: npm run lint:openapi

      - name: Build Docker image
        run: docker build -t fit4110/notification-service:lab04 .

      - name: Run Docker container
        run: |
          docker run -d --rm \
            --name fit4110-notify-lab04 \
            -p 8000:8000 \
            --env-file .env.example \
            fit4110/notification-service:lab04

      - name: Wait for service health
        run: npx wait-on http://127.0.0.1:8000/health --timeout 30000

      - name: Start A6 Core Mock Server
        run: npm run mock:core &
        
      - name: Wait for Mock Server
        run: npx wait-on http://127.0.0.1:4011/alerts/0196fb3d-4ad7-7d1e-9f49-5d5148d2babc --timeout 15000

      - name: Run Newman against Docker container
        run: npm run test:local

      - name: Show container logs on failure
        if: failure()
        run: docker logs fit4110-notify-lab04 || true

      - name: Stop container
        if: always()
        run: docker stop fit4110-notify-lab04 || true

      - name: Upload Newman reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lab04-newman-reports
          path: reports/
